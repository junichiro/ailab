# タッチスクロールを有効にする（スマホSSHクライアント用）
set -g mouse on

# コピーモードでviキーバインドを使う
setw -g mode-keys vi

# ステータスバーの位置
set -g status-position bottom

# エスケープキーの遅延を短くする（スマホでの反応向上）
set -sg escape-time 1

# プレフィックスキーを変更（オプション）
# set -g prefix C-a
# unbind C-b

